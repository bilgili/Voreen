# Adds all modules that are to be included by default (VRN_MODULES += default).
#
# These modules have to be stable, must not depend on external libraries 
# (except those that are included in the public releases for *all* platforms), 
# and have to be included in the public releases (source and binary).
#
VRN_MODULES += base
VRN_MODULES += connectedcomponents
VRN_MODULES += flowreen
VRN_MODULES += pvm
VRN_MODULES += triangle
